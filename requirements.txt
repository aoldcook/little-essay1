# Pinned dependencies for reproducible runs (EVAL_VALIDITY_AUDIT.md finding M2).
# Install with:  pip install -r requirements.txt
#
# torch is intentionally pinned loosely on the minor version because the correct
# build depends on the host CUDA version. Record the resolved version via
# `python -m repro.manifest` -- the manifest captures exact installed versions.

torch>=2.2,<2.9
transformers>=4.40,<5.0
numpy>=1.24,<3.0
scikit-learn>=1.3,<2.0

# Downstream reader LLM (Alibaba Bailian / DashScope OpenAI-compatible endpoint)
openai>=1.30,<3.0

# Optional: .env loading. The project ships a dependency-free fallback loader in
# evaluation/env_loader.py, so this is convenience only.
python-dotenv>=1.0,<2.0
